# Supabase Configuration
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Server Configuration
PORT=3001

# LLM Provider: "openai" | "deepseek" | "openrouter"
LLM_PROVIDER=openrouter
LLM_MODEL=openai/gpt-4o

# OpenRouter (recommended – single key for all models)
OPENROUTER_API_KEY=

# Direct provider keys (only needed if LLM_PROVIDER is set to that provider)
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
