# Dependencies
node_modules/

# Build outputs
dist/
build/

# Environment files
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo

# secret backups (never commit)
.env.bak*
backend/.env.bak*
*.env.bak*
database/backups/
frontend/.env.production
# Täckningsrapporten ska inte in i repot. (Raden hade tidigare en kommentar på samma
# rad — det stöds inte av gitignore, så mönstret matchade aldrig.)
backend/coverage/

frontend/test-results/
frontend/playwright-report/

# SEC-06: rotationslappar innehåller LIVE-hemligheter — får aldrig committas
ROTATION_*.txt
_to_delete/

# Supabase-CLI:ns scratch-state. Innehall bl.a. en projektreferens som inte
# hor hemma i repot (den som lag har pekade pa ett projekt som inte finns).
supabase/.temp/

# Byggrester och lokala verktyg (städat 12 sep)
__pycache__/
frontend/.netlify/
*.timestamp-*.mjs

# Claude-appen skriver hit när den sparar filer till mappen. Inte repomaterial.
Claude outputs/

# Säljunderlag med media (mp4, png, xlsx) — 55 MB för Wolfstreet ensamt. Ligger på
# disk, inte i git. Texten (samtalsunderlaget) finns i docs/SALES_*.md.
sales/
