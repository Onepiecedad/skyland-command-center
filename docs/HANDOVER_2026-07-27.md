# HANDOVER 2026-07-27 — Annonssystemet: ad-intel levererad, första scrapen körd

**Syfte:** Ge nästa tråd full kontext om annonssystemet för tatueringsstudios.
Bygger på förra handovern (annonssystem-planen, "två skills + subagent + tabeller").
Steg 1 och 2 i den planen är nu KLARA. Nästa tråd börjar vid steg 3 (`ad-factory`).

---

## 1. Vad som levererades (allt i drift + pushat)

- **Skill `ad-intel`** — `openclaw-config/skills/ad-intel/` (SKILL.md + `scripts/ad_intel.py`).
  Synkad till `~/.openclaw/skills/` via `sync_skills.sh ad-intel` + gateway-omstart.
  Commit `10c9127` i openclaw-config.
- **Tabell `ad_library`** i SCC:s Supabase (`wfwqjxsuvbacvcmpiesl`). Migration:
  `supabase/migrations/20260727_create_ad_library.sql`, APPLICERAD i prod.
  RLS PÅ, inga policies = endast service-rollen läser/skriver (medvetet — enda
  kärntabellen med RLS-skydd). Commit `f45cf3b` i skyland-command-center.
- **Första scrapen körd:** 681 annonser i tabellen (400 tattoo + 281 beauty,
  scope `se`, kostnad ~$3–4 Apify). Apify-krediterna fungerar igen (förra
  handoverns "kredit slut" är inaktuell).
- Env-krav för lokala körningar: `APIFY_API_TOKEN`, `SUPABASE_URL`,
  `SUPABASE_SERVICE_ROLE_KEY` i `~/.openclaw/.env` (Supabase-värdena = samma som
  `backend/.env`).
- **Skill-routing fixad (viktigt):** första försöket triggade `scc-crm` istället
  för `ad-intel` — ordet "tatuerare" matchade CRM-skillens description hårdare,
  varpå Alex webbsökte och svarade "jag skulle behöva kolla Ad Library". Löst
  genom att göra ägarskapet explicit i BÅDA descriptionerna: `ad-intel` inleder
  med "ÄGER ALLA frågor om vem som ANNONSERAR" + ordagranna svenska triggerfraser
  och förbud mot webbsök; `scc-crm` fick en avgränsning ("GÄLLER INTE frågor om
  vilka som annonserar — ägs av ad-intel") plus ett överlämningsstycke överst i
  kroppen. **Lärdom: när två skills överlappar i domänord räcker det inte att
  skärpa den nya — den vinnande skillen måste peka vidare.**
- **VERIFIERAT I DRIFT (2026-07-27 12:2x):** Alex i gateway-dashboarden kör nu
  skillen, läser `ad_library`, korsrefererar mot CRM och levererar färdig rapport
  med säljargument. Hela kedjan (skill → Supabase → gateway → UI) bevisad.

Kommandon (detaljer i skillens SKILL.md):

```bash
# Rapport ur befintlig data — GRATIS, alltid förstahandsval
python3 ~/.openclaw/skills/ad-intel/scripts/ad_intel.py --report --vertical tattoo
# Ny sweep (~$2 per 2 termer × 1 land) — max veckovis per scope
python3 ~/.openclaw/skills/ad-intel/scripts/ad_intel.py --vertical tattoo --scope se "tatuerare" "tattoo"
```

## 2. Medvetna avvikelser från förra handovern

1. **Actor:** `apify~facebook-ads-scraper` ($4–6/1000) — INTE `brilliant_gum`
   ($15/1000). brilliant_gums "unika" daysRunning-fält är bara `nu − startdatum`,
   vilket `ad_intel.py` (och befintliga `ads_pipeline.py`) redan beräknar själva.
   Samma data, 3× billigare, redan beprövad i stacken.
2. **Rollfördelning mot scc-crm:** `scc-crm/scripts/ads_pipeline.py` finns kvar och
   äger per-kort-kollen (stämplar `runs_ads` på CRM-kort). `ad-intel` är bulk-
   marknadsspaning och rör ALDRIG CRM-kort. Ingen duplicering — olika jobb.
3. **Skrivväg:** direkt Supabase PostgREST med service-nyckel (upsert på
   `ad_archive_id`), inte via SCC-backendens API (inga endpoints byggda — behövs
   inte förrän UI ska visa datan).

## 3. Terrängbedömning (steg 2 — AVKLARAD, bygg på detta)

**Tattoo SE — luckan bekräftad, med ett guldkorn:**
- 40 aktiva annonser på söktermerna, nästan allt brus (tattoo-appar, apparel,
  utländska produkter). Riktiga svenska studios som annonserar aktivt: ~6 i hela landet.
- Korsmatchat mot CRM:ets 65 tattoo-kort: **exakt 1 annonserar — Gothenburg Tattoo.**
  Deras annons: 229 dagar aktiv, ordagrant MEXPAND-mallen ("GÖTEBORG — Vi söker 10
  personer... 2000 kr rabatt + GRATIS konsultation").
- Tre slutsatser: (a) pitchsiffran är "av 65 studios annonserar 1 — här är luckan",
  (b) kursens copymall är BEVISAT lönsam i Göteborg i 7+ månader (körtid = proxyn),
  (c) konkurrensflagga: någon kör redan playbooken åt Gothenburg Tattoo.
- Kuriosa: Malte Erikssons kursannons syntes själv i datan (49 dgr aktiv).

**Beauty SE — mönstervolymen finns här, som förra handovern gissade:**
- Långkörare från riktiga kliniker: MediQ Clinic **987 dgr** ("Hej Stockholm 👋
  50% RABATT tills du blir permanent hårfri"), Vivaci 299 dgr, Viso Clinic 189 dgr
  (Örebro, tre varianter av samma struktur).
- Vinnarformeln är samstämmig och identisk med kursmallens struktur:
  **stad-callout + procentrabatt + "vi letar efter dig som" + video.**

**Konsekvens för steg 3:** brief-fasen i ad-factory ska utgå från den bevisade
SE-formeln, inte internationella mönster. Nordisk/intl sweep kan vänta.

### 3b. TVÅ RESERVATIONER INNAN SIFFRAN ANVÄNDS I SÄLJSAMTAL

Alex första rapport formulerade "64 tatueringsstudios i Göteborg — bara 1
annonserar". Två fel som måste rättas innan pitchen:

1. **Geografin stämmer inte.** Korten ligger i Göteborg, Mölndal, Borås,
   Kungälv OCH Lerum. Säg "i vårt upptagningsområde", eller kör en smalare
   geo-analys per ort. Pitchar man "i Göteborg" mot en Boråsstudio faller
   argumentet vid första motfrågan.
2. **"Ej hittad" ≠ "annonserar inte".** Sweepen sökte på orden "tatuerare" och
   "tattoo". En studio som annonserar utan de orden i annonstexten syns inte.
   Innan Tier A-studios pitchas: verifiera per kort med
   `ads_pipeline.py "Pistolero" "Crazy Colour" ...` (söker på studionamnet och
   stämplar `custom.runs_ads`). Först då är siffran vattentät.

## 4. Affärsbeslut (NYTT — stänger öppen fråga 1)

**Studion betalar annonskostnaden. Joakim tar 10 % provision.**
OBS: DM-doktrinerna (`dm-stil.md`, `dm-stil-beauty.md`) och äldre handovers säger
15–20 % — INTE uppdaterade än. Nästa tråd: bekräfta med Joakim vilken siffra som
är kanon innan doktrindokument röres.

## 4b. STÄDLISTA EFTER OPENCLAW-UPPGRADERINGEN (nytt, icke-trivialt)

Under sessionen tvingades OpenClaw uppgraderas 2026.6.6 → **2026.7.1-2** (Node
24.13 → **24.18** via nvm). Orsakskedjan: WhatsApp-kanalen var död sedan helgen
→ pluginet krävde runtime ≥2026.7.1 → nya OpenClaw krävde Node ≥24.15 → gamla
LaunchAgent pekade på gammal Node och gatewayn vägrade starta → `openclaw doctor
--repair` lagade tjänstefilen. Gatewayn är nu frisk (running, probe ok, CLI och
gateway båda 2026.7.1-2). Men reparationen lämnade fyra öppna punkter:

1. **WhatsApp-kanalen är trasig.** Pluginet är uppdaterat (2026.6.5 → 2026.7.1)
   och laddat, men loggen visar `[whatsapp:default] health-monitor: restarting
   (reason: stopped)` var 10:e minut — kanalen dör direkt efter start.
   Inkommande meddelanden processas därför aldrig. Konfigen är verifierat
   intakt (allowFrom, dmPolicy allowlist, selfChatMode true). Sessionsstaten
   ligger i `~/.openclaw/state/openclaw.sqlite` (ingen separat auth-mapp), och
   2026.7.1 exponerar INGET `openclaw whatsapp`-kommando för omlänkning.
   Nästa steg: `openclaw plugins inspect whatsapp --runtime --json`,
   OpenClaws dokumentation/support för hur man länkar om i 2026.7.x.
   **Alex nås under tiden via gateway-dashboarden `http://127.0.0.1:18789/`.**
2. **Webbsök-verktyget är trasigt** — "Tool error Web Search" i chatten.
   `exa-web-search-free` är ett av tilläggen doctor stängde av (se punkt 3).
3. **Doctor stängde av 15 tillägg** som inte var kompatibla med nya plugin-API:t:
   antigravity-agent, canva, clawdhub, clawhub-monitor, coding-agent,
   **exa-web-search-free**, goplaces, mcporter, model-usage, oracle,
   **scrapling**, session-logs, summarize, tmux, wacli. Inget är raderat — de
   är satta `enabled:false`. `scrapling` är särskilt viktig (research-flödet är
   Scrapling-omkopplat sedan 23/7): förvänta dig att prospect-research beter sig
   konstigt tills den uppdaterats och slagits på igen. Ta dem en i taget.
4. **Live-konfigen är ur synk med repot.** Doctor skrev om
   `~/.openclaw/openclaw.json` (backup: `openclaw.json.bak`). Repots
   `openclaw.json` är ÄLDRE. **Kör INTE `deploy_openclaw_config.sh` innan
   repot uppdaterats** — då skrivs doctors fixar över med gammalt läge.
   Kosmetiskt kvar: gatewayn varnar att `plugins.allow` är tom (whatsapp
   auto-laddas) och att service-PATH innehåller nvm — båda ofarliga.

Positivt ur samma körning: skills-lagret är helt friskt (123 skills, 87 Ready)
och `ad-intel` visas grön/Ready för agent `skyland`.

## 5. Öppna frågor kvar

1. **Attribution bokning→annons** (SCC-36) — fortfarande olöst, affärskritisk för
   provisionsfakturering. Utan den går 10 % inte att fakturera trovärdigt.
2. **Meta-policy för tattoo/kroppsmodifiering** — ej kollad. Ska göras innan
   första kampanjen går live.
3. Kanon-siffran för provision (se punkt 4).

## 5b. VAR ALEX FAKTISKT NÅS (tre ytor, olika förmågor)

Detta kostade en timme att reda ut — dokumenteras så det inte upprepas:

| Yta | Vem svarar | Skills? | Mobil? |
|---|---|---|---|
| Chattbubblan på scc.skylandai.se (⌘J) | Master Brain på Render (`backend/src/llm/tools.ts`) | NEJ — bara CRM-verktygen | Ja |
| Alex-fliken i SCC | Gatewayn via webbläsaren (`controlUi.allowedOrigins` innehåller scc.skylandai.se) | Ja, men räknaren visar felaktigt "0 skills" | Nej |
| **Gateway-dashboarden `127.0.0.1:18789`** | Gatewayn direkt, agent `skyland` | **Ja — verifierat** | Nej |
| WhatsApp | Gatewayn | Ja | Ja — men kanalen är trasig, se 4b |

Gatewayn är `bind=loopback` — därför fungerar de gatewaybaserade ytorna bara
från Macen. Vill man ha annonsdata även från mobil är den rena vägen ett
`query_ad_library`-verktyg i SCC-backendens `tools.ts` (Master Brain och
`ad_library` bor i samma Supabase) — ca 1 timmes bygge, ingen gateway inblandad.

## 6. Nästa steg: steg 3 — `ad-factory`

Skill som besvarar "gör fyra annonser till studio X". Ingångar som redan finns:
- Studiodata: `prospect_pipeline`-research + `studio-site-pipeline` (IG-scrape med
  engagemangsanalys — den kreativa guldgruvan).
- Vinnarmönster: `ad_library` (`--report`, eller SQL på `days_running`).
- **Copymall: `content-pipeline` finns redan** (MEXPAND modul 6, `annons-stil.md`
  som LAG, kodvalidering av siffror). ad-factory ska BYGGA PÅ/utöka
  content-pipeline — inte duplicera den. Trolig form: ad-factory orkestrerar
  (studiodata + vinnarmönster + brief) och content-pipeline producerar copyn.
- Krav från planen: fyra KONCEPTUELLT olika annonser (inte fyra hookvarianter),
  EN CTA, studions egna verk + artisternas ansikten.

**Röda linjer (oförändrade):** aldrig AI-genererade tatueringsmotiv; ingen
skrivåtkomst till kunders annonskonton; inga färdiga skill-paket; ingen ny
permanent MCP (Metas MCP omvärderas först efter steg 3, read-only, separat session).

## 7. Triage av nya "annonsguru"-skärmdumpar (oförändrad instruktion)

Utred INTE på nytt. Tre frågor: (1) Nytt verktyg eller ny paketering av samma
loop (scrape → vinnare → mönster → generera)? (2) Metodikdetalj som saknas i
planen? (3) Vem tjänar på att han sparar det? Researchen är klar — nästa
informationsbit finns i datan, inte i fler inlägg.
