# Handover — 2026-07-14: Deploy live på Render + F2 påbörjad + Alex full CRM-access

> Operatör: Joakim (joakim@skylandai.se)
> Föregående: `docs/HANDOVER_2026-07-13.md` (prospektering + DM-rader), `docs/TICKETS_F2_COMMS.md` (F2-plan).

## 1. Deploy — SCC kör nu i molnet (SCC-28 ✅)

- **Render Web Service `scc`**, Frankfurt, Starter ($7/mån), Docker via `backend/Dockerfile`
  (bygger frontend + backend i en container). Service-ID `srv-d9av0n1kh4rs73c6mrtg`.
- URL: `https://scc-e8x1.onrender.com` — `/health` verifierad 200 utifrån.
- Custom domain **scc.skylandai.se**: Verified i Render. one.com: gamla A-posten
  (207.154.220.13) BORTTAGEN, CNAME → `scc-e8x1.onrender.com` skapad, globalt propagerad.
- **VÄNTAR VID SESSIONSSLUT:** TLS-certet — första utfärdandet failade (DNS hann inte
  propagera), Render auto-retry:ar (upp till ~1h). Ta INTE bort/återlägg domänen — det
  fördröjer bara (Renders egen varning).
- Env: hela `backend/.env` importerad + korrigerad (`BACKEND_URL`/`SCC_PUBLIC_BASE_URL` →
  https://scc.skylandai.se) + nya EMAIL_*/OUTBOUND_*-vars. `OUTBOUND_ENABLED=false` (kill switch AV).
- Health check path: `/health/live`. Auto-deploy: På (varje push till main deployar).
- **Känd bugg i molnet:** Supabase *realtime*-subscriptions timar ut från Render
  ("Max reconnect attempts reached", tabeller skills/system_events). Påverkar bara
  live-uppdateringar i dashboard-vyn — API/CRM/Alex opåverkade. Ej åtgärdad.
- **SPA:n på Render saknar VITE_-tokens** (bakas vid byggtid, ligger inte i repot) —
  dashboarden körs lokalt mot Render-API:t tills riktig användarauth finns. Medvetet val.

## 2. Cutover ✅ GENOMFÖRD (senare samma session)

1. ✅ TLS-cert utfärdat — `https://scc.skylandai.se/health` ger 200.
2. ✅ n8n Notify-URL bytt i BÅDA workflows (void-submission + voice-call-ended) från
   ngrok → `https://scc.skylandai.se/api/v1/leads/intake`. Gjort via n8n Cloud-API:t
   (`onepiecedad.app.n8n.cloud` — OBS: n8n kör på n8n CLOUD, inte VPS:en; A-posten
   n8n.skylandai.se → 207.154.220.13 pekar på en död/annan server och används inte av flödet).
3. ✅ Test-lead postad mot nya URL:en → accepted, contact + activity skapade (testdata raderad).
4. ⬜ ENDA KVAR: Joakim stänger ngrok-processen på sin maskin (behövs inte längre).

## 3. E-postinfra (SCC-29 ✅ setup, väntar verifiering)

- **Resend-konto: joakim123 (gmail-kontot!)** — INTE skylandai-workspacet, eftersom free tier
  = 1 domän/konto och skylandai-kontot har marinmekaniker. Konsolidering till Pro ($20/mån)
  = F4-punkt när betalande kunder finns.
- Domän `send.skylandai.se` (Ireland/eu-west-1) tillagd. Alla 4 DNS-poster inlagda i one.com
  (MX + SPF på `send.send`, DKIM på `resend._domainkey.send`, DMARC p=none på `_dmarc.send`).
- **VÄNTAR VID SESSIONSSLUT:** Resend-verifieringen (Pending → Verified, DNS-propagering).
- `RESEND_API_KEY` finns i backend/.env OCH i Render-env.
- Körbok: `docs/EMAIL_INFRA.md` (volymtrappa, avsändaradress, verifieringssteg).

## 4. SCC-30 outbound e-post ✅ (byggd, avstängd via kill switch)

Commit `e7ed4c9`: migration `ticket30_outbound.sql` (applicerad i Supabase — status +
provider_message_id på messages), `services/email.ts` (Resend-adapter, factory-mönster),
`services/comms.ts` (executor: kill switch → daglig budget → kontakt-lookup → skicka →
logga message i unified inbox), `comms:email`-gren i taskService-dispatchen (endast
godkända tasks). `email.test.ts` 7/7 gröna. Flöde: task (executor comms:email, input
{contact_id, subject, body}) → approve → skickat + loggat.

**Första testmailet:** när Resend är Verified → sätt `OUTBOUND_ENABLED=true` i Render →
skapa task → approve → verifiera i inbox + Resend-loggen → SPF/DKIM/DMARC-pass i Gmail.

## 5. Alex — full CRM läs/skriv (byggt 2026-07-13→14)

Nya/uppgraderade verktyg i `llm/tools.ts` (commits `0360c64`, `7167343`, `b39b9ad`):
- `get_contact` — ALLA fält (score/tier/booking_flow/IG/adress/dm_hook + källa) + pipeline-läge via opportunities-join.
- `list_contacts` — tier-filter (A/B/C), score-sortering, custom/tags i svar.
- `list_opportunities` — pipeline/stage/status-filter, delnamnsmatchning, score-sorterad, opportunity_id för kedjning till move.
- `update_contact` — whitelistade fält, custom-MERGE (null raderar nyckel), add/remove tags, activity-loggas. Inga delete-verktyg (medvetet).
- Frontend: adress på prospekt-korten som Maps-länk (`18791d7`), custom.address satt på 6 av 7 tier A.

## 6. Prospektering (från 13/7, oförändrat läge)

- Tier A efter verifiering: **Rita Simonn 95 · Crazy Colour 93 · Skindiver 87 · Ivory Tower 87**.
  TELLO 83/B + Unikum 78/B (form). Ink Brothers 57/C — har redan Bokadirekt-onlinebokning, annan pitch.
- DM-rader klara för review: `ai-agency-course-extract/dm-rader-tier-a-batch1.md`.
  Hooks + källor i `contacts.custom.dm_hook`. **Joakim har inte skickat än.**

## 7. Öppna trådar, prioriterat

1. Cutover-stegen i §2 (cert → n8n → test-lead → ngrok av).
2. Resend Verified → första testmailet genom approve-flödet (§4).
3. DM-utskick tier A (manuellt via IG, kön finns).
4. SCC-31 SMS/46elks + SCC-34 Alex send_email/send_sms (SUGGEST-only) — nästa byggen.
5. Realtime-buggen från Render (låg prio).
6. Säkerhet före OUTBOUND_ENABLED=true på riktigt: SCC-35-checklistan i `TICKETS_F2_COMMS.md`.

## 8. Drift-noteringar

- Push sker från Joakims terminal (Antigravity IDE, repot öppet där). Auto-deploy på Render vid push.
- Git-lock-egenheten kvarstår (mv *.lock). Supabase-MCP ansluts per session.
- Render-kontot: Joakims workspace (dashboard.render.com). one.com DNS: skylandai.se.
- Vercel/Netlify-sajterna orörda (www + dashboard pekar fortfarande på Netlify).
