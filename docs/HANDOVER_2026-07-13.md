# Handover — 2026-07-13: Prospekterings-spår (37 scorade tatuerar-leads + CRM-kort)

> Operatör: Joakim (joakim@skylandai.se)
> Läs detta först vid nästa session. Bakgrund: `docs/HANDOVER-CRM-F1-och-leadlista.md`.

---

## 1. Vad som gjordes

Byggde Joakims **egna kundanskaffnings-lager** ovanpå F1-CRM:t. Mål: hitta tatuerarstudior och
boka säljmöten med dem (Joakims agency ska ha kunder innan systemet säljs vidare). Detta är
**Joakims prospekterings-pipeline**, INTE kundens MEXPAND-leveransflöde — blanda inte ihop dem.

## 2. Data i Supabase (`wfwqjxsuvbacvcmpiesl`, skarpt)

- **Ny pipeline `Prospecting (Agency)`** — `pipelines.id = 2f3e8859-6294-42a0-9cd7-f712155091e9`,
  `customer_id = null` (agency-nivå, skild från klienterna thomas/axel/gustav och från default "Sales").
  8 stages: New Prospect · Qualified · Outreach Ready · Contacted · Replied · Meeting Booked · Won · No Fit.
- **37 tatuerarstudior** (32 Göteborg + 5 Mölndal), skrapade via Google Maps (Chrome-MCP,
  flera smala sökningar). Endast tatuerare — PMU/barber rensades bort på Joakims begäran.
  Varje studio = en `contact` (`source='google_maps'`, `dedupe_key='gmaps|<namn>|<area>'`) + en
  `opportunity` i New Prospect.
- **Berikning** (via websök per studio): 36 Instagram, 21 webb, 16 mail, 31 telefon.
  Lagrat i `contacts.custom`: `instagram`, `email`, `website`, `rating`, `reviews`, `niche`, `area`.
- **Nyckelinsikt:** tatuerare bokar nästan uteslutande via **Instagram DM**, inte mail. Outreach
  ska vara IG-DM-först.

## 3. Scoring

Sparat i `contacts.custom.score` (0–100) + tagg `tier:A/B/C`. Modell:
- Volym 0–35 (antal omdömen) · Kvalitet 0–15 (betyg) · Bokningsflöde 0–50.
- `custom.booking_flow`: `manual` (DM/telefon, 50) · `form` (förfrågningsformulär, 35) · `online` (självbetjäning, 12).
- Sajterna kollades individuellt (web_fetch). **Ingen studio har riktig online-bokning** — allt är
  DM/telefon/formulär. Det betyder att hela nischen har exakt det gap Joakims tjänst löser (stark pitch).
- 7 sajter lästes i klartext; ~12 var JS-renderade/ohämtade och fick default `form` (ej individuellt verifierat).

**Tier A (ring/DM först):** TELLO 98 · Ink Brothers 95 · Rita Simonn 95 · Unikum 93 · Crazy Colour 93
· Skindiver 87 · Ivory Tower 87.

CSV-backup: `ai-agency-course-extract/leads-prospecting-batch1.csv`.

## 4. Frontend (pushat)

Commits på `main`: `bb3aa99` (kort-fält) + `4d81b4c` (sort/filter).
- `backend/src/routes/pipelines.ts` — board-endpoint skickar nu `contact.custom/phone/tags`.
- `frontend/src/api.ts` — nya typer `ContactCustom` + `OpportunityContact`.
- `frontend/src/components/PipelineBoard.tsx` — kort visar score-badge (grön A ≥85 / gul B ≥70 / grå C),
  `@instagram`, betyg (omdömen), bokningsflöde + kanal. Toolbar: sortera på score + filtrera på tier.
- `tsc --noEmit` grönt i både frontend och backend.

**Så ser man det:** kör SCC lokalt (`npm run dev` i backend + frontend) → flik **CRM** → dropdown
uppe till höger → **"Prospecting (Agency)"**. CRM-vyn defaultar annars till "Sales" (is_default).

## 5. GHL-MCP (verifierat mot officiell doc)

- Endpoint: `https://services.leadconnectorhq.com/mcp/`. Auth: Private Integration Token som
  `Authorization: Bearer pit-...`-header + `locationId` (Joakims sub-account `QXB9Atyp12wDXZRMboPJ`).
- 36 verktyg. **Write finns för data** (kontakter, tags, opportunities, conversations/meddelanden)
  men **inte för struktur** (pipelines, custom fields, workflows, kalendrar, snapshots) — det är GHL:s
  admin-lager (kräver UI eller bredare scope). Strukturen löses via snapshot (Joakim har AAC-snapshots importerade).
- Ej ansluten i denna sessions toolset; ligger inte i Claudes connector-register (custom connector).
  HighLevels roadmap nämner npx-wrapper för klienter (inkl. Claude Desktop) som inte stöder rå HTTP-MCP;
  n8n är beprövad fallback.

## 6. Öppna trådar / nästa steg

1. **DM-öppningsrader för tier A** (7 studior) — snabbaste vägen till första mötet.
2. Ev. skala listan mot ~100 (fler områden).
3. Verifiera de ~12 JS-sajternas bokningsflöde om exaktheten spelar roll.
4. Bygg outreach-modul med IG DM som huvudkanal (human-in-the-loop: generate → review → approve).
5. Kvarstår från tidigare: **F2** (utgående mail/SMS/kalender), samt säkerhetsskuld **RLS av** +
   frontend-token-läcka före extern kunddata.

## 7. Att veta (drift)

- Externa MCP:er (Supabase, GHL, Chrome) ansluts per session — verifiera innan operation.
- Mountad `.git` tillåter inte `unlink`; git lämnar kvar `*.lock` (ofarligt). Push från Joakims terminal.
