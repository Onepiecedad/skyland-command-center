# HANDOVER — 2026-07-23

> Ny session? Läs `docs/HANDOVER_2026-07-18.md` (prospekteringsmaskinen) + `docs/HANDOVER_2026-07-19.md`
> (röststyrning/mobil/IG-autologg) först, sen detta.
> Kort: erbjudandet bytte affärsmodell, Alex kan nu PRODUCERA komplett material själv (4 annonser +
> 30s film), och research-flödet blev token-snålt via Scrapling. Sälj-maskin → sälj- + leverans-maskin.

## Var vi står

Prospekteringsmaskinen rullar vidare (säljläge per 07-19-handovern gäller som utgångspunkt). Ovanpå
den landade fyra saker denna period: **(1)** erbjudandet bytte till annonsbudget-modellen, **(2)** ett
material-arkiv per studio (Fas 2), **(3)** Alex kan producera komplett annonsmaterial på kommando
(Fas 3, `produce_package`), och **(4)** research-flödet kopplades om till Scrapling för billigare,
token-snål sajt-scraping. Borås tillfördes som ny ort (12 studior, hela vägen till DM-öppnare).

## Vad som byggdes

### Affärsmodell bytt — annonsbudget-modellen
Gamla "ren provision / jag tar risken" ut. Nya: **studion sätter en liten annonsbudget, jag tar
provision/andel per bokad sittning, inget fast.** Alla 11 Outreach Ready-pitchar omskrivna.
Doktrinen (`dm-stil.md`) + validatorn (`dm_pipeline.sh`) låsta till modellen: "annons" är inte
längre förbjuden mekanik, `provision|andel` krävs i pitchen. (Drivet av: "jag vill inte betala för
annonsen".)

### IG-DM härdat (två buggar)
- **n8n-autosvar fyrade i prospekterings-tråd** (nyckelord-matchade "vecka"). Fix: exakt matchning
  (`norm===kw`), "vecka" borttaget, tankstreck rensade.
- **Webhooken tappade meddelanden tyst vid handle-mismatch** (Valhalla lagrad `juanes.art`, svarade
  från `valhalla_ink_studio_`). Fix: matcha på IG **numeriskt id** först, self-heal (spara `ig_id`
  vid träff), och vid no-match logga `ig_dm_unmatched` (warning) i st f tyst drop. Handle rättad +
  backfill kört.

### Fas 2 — material-arkiv per studio
- Ny tabell `studio_assets` + privat Storage-bucket `studio-material` (signerade URL:er).
- Route `studioAssets.ts` (lista / upload-url / signed-url / radera). Stora filer laddas DIREKT till
  Storage via signerad upload-URL (kringgår Renders 10 MB-JSON-gräns).
- **Material-flik** på kontaktkortet (`MaterialPanel.tsx`).
- Buggar lagade: upload-URL dubbel-prependade Supabase-basen (502); CSP blockerade video
  (`mediaSrc` += `https:`); HTML öppnas via text/html-blob (Supabase tvingar text/plain).

### Fas 3 — Alex producerar material själv
- Nytt Master Brain-verktyg **`produce_package`** → review-task → OpenClaw-agent `produce-package`:
  Apify (studions IG-material) → **4 annonser (ImageMagick) + 30s film (ffmpeg)** → upp till
  studio_assets. Annonsfont brandad till **Anton** (system-font-fallback).
- Nytt verktyg **`find_prospects`** (discovery via chatt → `prospect-finder`-agent).
- Kört skarpt end-to-end på **All Gold** (4 Anton-annonser + film, dubbletter rensade).

### Borås — 12 nya studior
discover_pipeline → research → DM-öppnare, hela vägen. Tre luckor lagade: (a) doktrinen hade gamla
modellen + tankstreck, (b) validatorn krävde "provision"/förbjöd "annons", (c) runtime saknade
`verticals/tattoo.json`. Efter fix: alla 12 kort med korrekta öppnare.

### Scrapling MCP — research-flödet omkopplat
- Installerat i `~/.scrapling-venv`, registrerat i mcporter (STDIO, 10 verktyg), ny skill
  `skills/scrapling/SKILL.md`.
- Research pekar nu på **`scrapling.get`/`stealthy_fetch` för studiornas sajter + bokningssystem/
  tech-koll** (token-snålt via CSS-selektorer + `main_content_only`, anti-bot-tåligt) i st f de
  betalda Apify-actorsen (techstack/pagespeed/facebook).
- **Apify behålls** för: IG-media (produce_package), Google Maps (discover), Google-recensioner
  (svårscrapat — skött actor mer tillförlitlig).
- Uppdaterade dokument: `tattoo_brief.md`, `beauty_brief.md`, `RESEARCHER_SPEC_V1.md`,
  `TOOL_RUNTIME_BASELINE.md`.

## Att veta / öppna trådar

- **SCC→OpenClaw-dispatch LÖST (pull-modell):** Render-molnet kan inte nå gatewayn på Macens
  localhost, så kopplingen är vänd. SCC-env `OPENCLAW_DISPATCH_MODE=pull` gör att dispatchern KÖAR
  claw-körningar (`task_runs.worker_id='pull:queued'`) i st f att pusha; en poller på Macen
  (`openclaw-config/scripts/scc_poller.py`) hämtar via `GET /api/v1/claw/pending`, avfyrar agenten
  lokalt (`/hooks/agent`), och agenten rapporterar tillbaka via `scc-callback` → `/claw/task-result`
  (oförändrat). Claim är atomisk (queued→claimed) så inget dubbelkörs; reapern fångar ej-avfyrade.
  **UPPDATERING 07-24: fullt driftsatt + testad end-to-end** (env satt, poller kör via launchd, sex
  grindar lagade). Se `docs/HANDOVER_2026-07-24.md`. Push-läget (default) är kvar för lokal körning.
- **openclaw-config auto-committar INTE** — Joakim kör `git add/commit/push` själv, och kopierar
  skills/agents/hooks till runtime manuellt (`~/.openclaw/...`). `deploy_openclaw_config.sh` synkar
  bara openclaw.json + cron/jobs.json.
- **Beauty-doktrinen** (`dm-stil-beauty.md`) behöver samma modell-/tankstreck-fix som tattoo — görs
  när beauty-vertikalen dras igång.
- **Kostnad:** OpenRouter-spiken ($3.20/45 min) var debug-omkörningar + freelance-timeout, inte läcka
  (produktiv research stämplad $0.44). Håll koll när research körs om i loop.
- **Säkerhet innan extern kunddata** (kvar från 07-19): rotera Supabase service-nyckel, RLS-policies,
  riktig användarauth (VITE-tokens publika).

*Skriven 2026-07-23.*

---

## Del 2 — parallell session samma dag (beauty-vertikal + annons-spaning)

> Byggt i separat Claude-session (Cowork) parallellt med ovanstående. Koden är MERGED —
> commit `f8d094e` byggde annonsbudget-fixen ovanpå vertikal-stödet — och allt är pushat.

### Beauty-vertikalen (BV-1–BV-7, se `docs/TICKETS_BEAUTY_VERTICAL.md`)
- Allt nischspecifikt flyttat ur skriptkoden till `openclaw-config/skills/scc-crm/verticals/<namn>.json`
  (söktermer, dedup-stoppord, exkludering, sajtsignaler, scoring, brief, DM-doktrin, pipeline).
  discover/prospect/dm/batch tar `--vertical` (default tattoo = bitidentiskt beteende);
  prospect/dm härleder vertikalen ur KORTETS niche-tag så fel doktrin aldrig körs mot fel kort.
- Ny pipeline **"Prospecting (Beauty)"** i Supabase (8 stages, samma stage-namn som Agency så
  auto-logg + IG-webhook funkar orört; `is_default` orörd). Migration
  `ticketBV2_beauty_pipeline.sql` — REDAN APPLICERAD i prod.
- Beauty-specifikt: multi-term-sök + exkluderingsfilter (frisör/naglar/spa listas som bortfiltrerade),
  sajtsignaler (`sells_packages`/behandlingsmix → custom), paketviktad scoring (paket 40p — GTM v2:
  provision beräknas på paketvärde), beauty-brief med IVO_STATUS/PAKET_PRISLISTA/BEHANDLINGSMIX,
  `dm-stil-beauty.md` med JURIDISKA kodgrindar (botox/läkemedelsvarumärken, resultatgarantier,
  kredit/delbetalning fäller utkastet).
- Buggar fixade på vägen: pipeline-valet matchade substring "Prospecting" (fel pipeline så fort det
  finns två), research-briefen hårdkodade "Göteborg/Mölndal" (nu kortets `custom.area`).
- **KVAR: BV-8-piloten** (beauty-discover dry-run Göteborg + tattoo-regressionskörning) innan skarp
  användning. **ÖPPET BESLUT (Joakim):** `dm-stil-beauty.md` är skriven mot GTM v2
  (provision på paketvärde, golv 1 000 kr, inga fasta avgifter) — ska beauty också över på
  annonsbudget-modellen? Grindar + doktrin uppdateras när beslutet är taget.

### ads_pipeline — Meta Ad Library-spaning (nytt skript)
- `--market "<sökord>"` → rapport + rådata i `~/clawd/out/ads-reports/` med **långkörarlista**
  (livslängd = vinnar-proxy; Ad Library visar aldrig prestanda för kommersiella annonser).
  Prospekt-läget stämplar `runs_ads`/`ads_checked`/`ads_notes` på korten (styr DM-vinkel Mall 1/2).
- **KÖRT SKARPT:** "tatuerare" SE → 300 unika annonser, 89 annonsörer. Fynd: rabattmallen
  ("2000 kr + gratis konsultation") är en RIKSMALL — identisk copy hos 8 annonsörer i 4 städer
  (Gothenburg Tattoo aktiv 225 dgr); trygghets-/berättarvinkeln är bevisad långkörare och LEDIG i
  Göteborg (Moment 48 annonser/Sthlm, Tequila); TELLO = expanderande kedja (3 städer, 3D-vinkel);
  Malte annonserar kursen aktivt (45 dgr) = fler mallbyråer på väg in.
- **5 kort stämplade** ur datasetet: Gothenburg Tattoo + TELLO (aktiva annonsörer), Wolfstreet,
  Dark Tattoos Sweden, Niklas Ejvind (testat & slutat). Övriga kort fick INGEN nej-stämpel —
  frånvaro i sökordsträff är inte bevis. Definitiv koll per kort: `ads_pipeline.py --all-unchecked`
  när kredit finns.
- **Apify-krediten SLUT** ($5.00/$5.00) — prospekt-koll, nya discover-orter och IG-research väntar
  på påfyllning eller månadsskiftet. Marknadskörningen hann igenom och är räddad.

### sync_skills.sh (`openclaw-config/scripts/`)
- Repo → `~/.openclaw/skills` med körningsvakt (VÄGRAR synka medan pipelines kör — torn file-läxan),
  raderar borttagna filer bara INOM repo-skills, och skyddar runtime-artefakter
  (`.venv`/`node_modules`/`.env`/loggar/`out`). **Ersätter manuell kopiering** — "kopierar skills
  manuellt" ovan är obsolet. Körs `bash scripts/sync_skills.sh [skill]`, sen gateway-omstart.
- Incident: FÖRSTA synken (innan skyddet fanns) rev delar av phone-voice `bridge/.venv` —
  återskapas via `requirements.txt` om skillen behövs.

### Exec-bitar (fixat i denna uppdatering)
- `dm_pipeline.sh` + `prospect_pipeline.py` tappade 100755 i dagens commits → prospect_pipelines
  DM-steg hade gett PermissionError. `chmod +x` återställt i repo — committa mode-ändringen och
  kör om synken så runtime får rätt rättigheter.

*Del 2 skriven 2026-07-24 (natt), Cowork-sessionen.*
